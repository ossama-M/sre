management:
  endpoints:
    web:
      exposure:
        include: '*'
  info:
    env:
      enabled: true


spring:
  application:
    name: inventory
    description: vois
    author: Ossama Mohamed
  profiles:
    active: prod
  jpa:
    show-sql: true